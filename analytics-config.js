/* ============================================================
   측정 설정 — GA4 측정 ID 슬롯
   ------------------------------------------------------------
   Google Analytics 4에서 속성을 만들면 받는 "측정 ID"(G-로 시작)를 아래에 붙여넣으세요.
   (analytics.google.com → 관리 → 데이터 스트림 → 웹 → 측정 ID)
   비워 두면 사이트는 정상 동작하며 수집만 하지 않습니다.

   내부 트래픽 제외: 사장님 기기에서 https://01mobile.co.kr/?gm_track=off 를 한 번 열면
   그 기기는 영구 제외됩니다(해제는 ?gm_track=on). 매장 사업은 운영자 접속이 지표를
   오염시키는 폭이 커서, 이 한 번이 데이터 신뢰도를 좌우합니다.
   ============================================================ */
window.GA4_MEASUREMENT_ID = "";
